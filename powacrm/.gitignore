node_modules/
dist/
.env
.env.local
*.local

# Carried over from create-vite@9.2.0's react-ts template .gitignore
# (deleted as a duplicate of the app/ scaffold's own file — these entries
# were not duplicates and need a home here instead).
dist-ssr
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.vscode/
.idea/
*.sw?
# Visual Studio artifacts — the template ignores these; kept for parity in
# case a contributor opens the app in VS/VS Code with those tools installed.
*.suo
*.ntvs*
*.njsproj
*.sln
